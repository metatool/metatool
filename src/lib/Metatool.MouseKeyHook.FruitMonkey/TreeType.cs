namespace Metatool.Input;

public enum TreeType
{
    Default,

    /// <summary>
    /// only one fruit allowed for every down, up or allUp KeyEvent, used for Chord map tree
    /// </summary>
    SingleFruitPerEventType
};

<Window x:Class="Metatool.ScreenHint.HintUI.MainWindow" AllowsTransparency="True" WindowStyle="None" Topmost="True" ShowActivated="False" ShowInTaskbar="False" Background="Transparent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Metatool.ScreenPoint"
        Title="KeyMouse Tool"
        mc:Ignorable="d" d:DesignWidth="478.076" d:DesignHeight="246.156"
>
    <Grid>
        <!-- Semi-transparent border rectangle; sits behind canvases, no layout impact Fill="#18FF0000"-->
        <Rectangle IsHitTestVisible="False" Stroke="Red" StrokeThickness="2" />
        <TextBlock x:Name="_loadingText" IsHitTestVisible="False" Visibility="Hidden"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="18" FontWeight="Bold" Foreground="#FFD700" Background="#CC333333"
                   Padding="16,8" Text="Detecting..." />
        <Canvas x:Name="_Canvas" Background="Transparent" IsVisibleChanged="_Canvas_OnIsVisibleChanged">
        </Canvas>
        <Canvas Background="Transparent" IsHitTestVisible="False">
            <Rectangle x:Name="highLight"  Stroke="Red" Visibility="Hidden" StrokeThickness="2"></Rectangle>
        </Canvas>
    </Grid>
</Window>
<!-- ShowActivated="True" is a WPF Window property that controls whether the window receives focus when it is first shown.

True (the default): The window is activated (brought to foreground and given keyboard focus) when Show() or ShowDialog() is called.
False: The window appears without stealing focus from the currently active window. -->
<Window x:Class="Metatool.WebViewHost.WebViewHost"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Metatool WebUI" WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinHeight="200" MinWidth="600"
        MaxHeight="900" MaxWidth="1400">
    <Window.Resources>
        <DropShadowEffect x:Key="DropShadow" BlurRadius="10" ShadowDepth="5" Opacity="0.3" />
    </Window.Resources>
    <Border Background="#F0F0F0" CornerRadius="12" Effect="{StaticResource DropShadow}">
        <Grid>
            <wv2:WebView2 x:Name="webView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </Grid>
    </Border>

</Window>

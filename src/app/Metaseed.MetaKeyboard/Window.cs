using System;
using System.Collections.Generic;
using System.Text;
using System.Windows.Forms;
using Metaseed.Input;
using static Metaseed.Input.Key;

namespace Metaseed.MetaKeyboard
{
    public class WindowKeys
    {
        // public IMetaKey LWinLeft     = (LWin + A).Map(Keys.Left);
        // public IMetaKey LWinDown     = (LWin + S).Map(Keys.Down);
        // public IMetaKey LWinUp       = (LWin + W).Map(Keys.Up);
        // public IMetaKey LWinRight    = (LWin + D).Map(Keys.Right);
    }
}
